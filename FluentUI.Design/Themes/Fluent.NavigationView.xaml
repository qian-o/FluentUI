<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:FluentUI.Design.Controls">

    <Style TargetType="{x:Type controls:NavigationView}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NavigationView}">
                    <Grid x:Name="Main">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="ColumnPane" MaxWidth="{TemplateBinding OpenPaneLength}" />
                            <ColumnDefinition x:Name="ColumnContent" />
                        </Grid.ColumnDefinitions>
                        <ContentControl Content="{TemplateBinding Content}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="DisplayMode"
                                 Value="Overlay">
                            <Setter TargetName="ColumnPane"
                                    Property="MinWidth"
                                    Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>